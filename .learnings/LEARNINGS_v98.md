# V98 Learnings

1. 每加入一条新的可信官网记录，都必须从头重算公开行的最小松弛；不能沿用旧的一分修复结论。
2. 当前 14 条可信官网行与八条公开行的最小 L1 正确题数松弛为 5，不是 1。
3. `reg_a_004=C` 和 `ABC` 都能达到最小松弛 5，公开行无法裁决；在没有单题官网结果前应保留 incumbent。
4. `fc_a_004=CD`、`fc_a_014=AB` 已由直接官网方程排除。替换这两个答案在精确匹配评分下不会降低正确题数。
5. 当前最有价值的首轮不是再改监管歧义题，而是联合测试 `fc_a_004=D` 与 `fc_a_014=C`，结果域严格限制在 95、96、97。
6. P1 返回 96 后必须用单题 P2 归因；P3 只能按归因结果选择，不能预先批量提交。
7. 题型合法性必须纳入公共约束。单选题 `fin_a_006` 的 D 不是新标签，而是必错的非法输出。

