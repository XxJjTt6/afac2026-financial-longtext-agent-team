# LEARNINGS V117

1. 长期 95 题平台期不能再仅视为候选答案排序不准，必须审计题目身份、数据版本和隐藏标签命名空间。
2. 官网当前介绍与本地下载数据对 `reg_a_014` 绑定了不同题干、不同文档 ID 和不同答案表达，这是一个已证明的 QID 冲突。
3. 官网样例公布 `AC` 不等于隐藏评测器必然使用 `AC`。正确方式是只改一题做可证伪检验，而不是将网页样例包装成真值。
4. 六次提交必须是自适应决策树：只有第一个文件无条件，后续文件必须由前一次实测正确题数解锁。
5. 水印 Token 能区分本地候选文件，但仍必须读取官网该次提交行，不能用“当前最佳”反推当次结果。
