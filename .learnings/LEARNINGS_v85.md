# LEARNINGS V85

## 新硬事实

- V84 P1 官网实测为 94 题，不再是未提交推导值。
- V84 相对 V36 净增 1，结合 `fc_a_004=AC` 已知错，可无条件证明 `res_a_006=B` 错。
- V84 的增益只可来自 `res_a_006=A` 或 `fc_a_004=CD` 之一，两者不能同时正确。

## 提交设计

- 剩余两次额度时，第一次应保留单题可归因性，而不是一次打包两个候选。
- `res_a_011=ABC` 同时具有外部批次支持、联合最小松弛方向支持和 +1 上行，适合用作 P1；但它仍有严格原文反证，不可宣称保证提分。
- `fc_a_004=A` 依赖外部 `res_a_006=A` 绑定来消除 `CD` 下行，因此只能作 P2，不能与 P1 同时盲投。

## 方法修正

- 加入新官网行后，必须重新计算公开行的最小松弛；V84 前的最小 L1=1 已变成 L1=2。
- 联合最小修复现在偏向 `CD`对、`res6=A`错，与外部隔离单题记录相反；来源家族隔离后硬结论再次消失。这进一步证明不能把互相矛盾的外部行联合拟合成标签。
- 官网页头最佳分可能滞后于最新结果行；证据登记应保留时间、结果行分数、本地 CSV 摘要和截图摘要。
