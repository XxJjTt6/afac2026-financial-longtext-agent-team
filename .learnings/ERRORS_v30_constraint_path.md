# V30 约束证明路径错误

## ERR-20260712-003 临时验证脚本使用了未解析的相对根目录

- 时间：2026-07-12
- 现象：首次生成 V30 条件约束证明时，题目集合为空并触发 `KeyError: fc_a_001`。
- 根因：临时脚本把 `Path('.')` 的父级仍当作项目父目录；该相对路径未先解析为当前工作目录。
- 处理：改用 `Path.cwd()` 构造绝对项目根目录后重新运行，成功载入100题并生成约束证明。
- 影响：首次失败发生在写出证明文件之前，未产生不完整结果。

